# Data Directory

This directory is reserved for synthetic, sanitized, or de-identified case materials used in TRACE experiments.

Suggested layout:

- `raw/` for source case materials that should remain local-only
- `processed/` for normalized text used in experiments
- `metadata/` for case manifests, prompt logs, and run descriptors

Do not commit sensitive student records or identifiable education documents.
